#include <stdio.h> // stdio.h 提供了 printf()
#include <stdlib.h> // 此行暫且不提

int main(int argc, char * argv[]){ // 圓弧括號內之內容暫且不提
	/* 單純輸出字串 */{
		printf("Ｃ程式語言世界你好！\n");
	}
	
	/* 輸出無正負號位(unsigned)整數(integer)型態資料 */{
		printf("3 + 3 = %d\n", 3 + 3);
	}
	
	return EXIT_SUCCESS; // EXIT_SUCCESS 暫且不提
}
